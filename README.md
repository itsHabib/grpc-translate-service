# gRPC Translate Service
gRpc + AWS SDKs + Rust cargo project for the [blog post]().
The gRPC service has two methods which can translate text, and synthesize text
to speech.

### TODO
- [ ] dockerize application
